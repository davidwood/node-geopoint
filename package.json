{
  "name": "geopoint",
  "description": "Representation of a geographic point for node.js and the browser",
  "version": "1.0.1",
  "author": "David Wood <bitprobe@gmail.com>",
  "repository": {
    "type": "git",
    "url": "http://github.com/davidwood/node-geopoint.git"
  },
  "keywords": [
    "geolocation",
    "point",
    "distance",
    "bounding box",
    "radius"
  ],
  "dependencies": {},
  "devDependencies": {
    "istanbul": "^0.4.5",
    "mocha": "^3.5.3"
  },
  "main": "./geopoint.js",
  "engines": {
    "node": ">= 0.8.0"
  },
  "scripts": {
    "test": "NODE_ENV=test istanbul test _mocha -- --check-leaks --reporter spec --recursive"
  }
}
